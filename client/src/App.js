import "./App.css"
import React from "react"
import { Box } from "@mui/material/"

const App = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexDirection: "column",
        gap: "25px",
        height: "100vh"
      }}
    >
      👀
    </Box>
  )
}

export default App
